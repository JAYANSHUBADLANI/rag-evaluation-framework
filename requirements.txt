numpy>=1.23
scikit-learn>=1.1
faiss-cpu>=1.7
PyYAML>=6.0
pytest>=7.0

# Optional, higher-quality dense embedding backend.
# Enable by setting `embedder.name: sentence-transformers` in the config.
# Requires a downloadable model the first time it runs.
# sentence-transformers>=2.2
